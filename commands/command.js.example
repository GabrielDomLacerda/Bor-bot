const { SlashCommandBuilder } = require('@discordjs/builders');

module.exports = {
    data: new SlashCommandBuilder()
        .setName(/*insira o nome aqui*/)
        .setDescription(/* insira descrição aqui */),
    execute: async function (interaction) {
        //codigo do comando
    },
};
